# KB Copilot 示例文档

KB Copilot 是一个面向中小企业的智能知识库问答助手。

系统支持上传企业内部文档，将文档解析为文本后进行切分，并通过 Embedding 模型转换为向量。向量和文档元数据会写入 Qdrant，用于后续语义检索。

当用户提出问题时，系统会先从 Qdrant 检索相关文档片段，再把片段作为上下文交给大模型生成回答。回答会同时返回引用来源，方便用户确认答案依据。

MVP1 阶段支持 Markdown 和 TXT 文档，重点验证文档上传、索引构建、语义检索和 RAG 问答主流程。
