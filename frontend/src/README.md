# Frontend Src

前端源码放在这里。

目录职责：

- `api`: 后端接口封装和请求客户端。
- `assets`: 图片、图标等静态资源。
- `components`: 跨页面复用的通用组件。
- `features`: 按业务模块组织代码，例如知识库、文档、问答。
- `layouts`: 应用布局和导航框架。
- `pages`: 路由页面。
- `router`: 路由定义。
- `styles`: 全局样式和主题变量。
- `types`: TypeScript 类型定义。
- `utils`: 通用工具函数。
